// import tree from 'data/characterTree';
// import BFS from '../BFS'

describe('BFS', () => {
    const answer = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J', 'K'];
    it('should return correct results', () => {
        // expect(BFS([tree], [])).toStrictEqual(answer);
    });
});
